// The event onboarding emits when it has established enough to research (§3.4).
// The CONSUMER — the Research pipeline entry — lives in @tethr/research
// (Constitution XII: research owns Ch 11 and its trigger seam). Onboarding only
// produces this event; it does not invoke the pipeline.
export const ONBOARDING_COMPLETED_EVENT = "onboarding.completed";
